<div class="row gtr-200">

	{% block about %}
		{% if config.site.metadata.about is defined %}
		<div class="col-12">
			<!-- About -->
				<section>
					<h2 class="major"><span>What's this about?</span></h2>
					{{ config.site.metadata.about|raw }}
				</section>
		</div>
		{% endif %}
	{% endblock %}

	{% block contacts %}
		{% set contacts_email = (config.theme.contacts.email is defined) and ('footer' in config.theme.contacts.email) %}

		{% if config.site.globals.social is defined or contacts_email %}
		<div class="col-12">
			<!-- Contact -->
				<section>
					<h2 class="major"><span>Get in touch</span></h2>
					<ul class="contact">
					{% for network, account in config.site.globals.social|default([]) %}
						{% set social_network = config.theme.social_networks[network] %}
						{% if social_network is not empty %}
							<li><a class="icon brands fa-{{ social_network.icon|default(network|lower) }}" href="{{ evaluate_twig(social_network.permalinks, {u: account}) }}"><span class="label">{{ social_network.label|default(network|capitalize) }}</span></a></li>
						{% else %}
							{{ dump("Warning: social network \"#{network}\" from site.yaml is not defined in theme.yaml, unable to show icon in footer.") }}
						{% endif %}
					{% endfor %}
					{% if contacts_email %}
						<li><a class="icon fa-envelope" href="mailto:{{ site.author.email|safe_email }}"><span class="label">Send us email</span></a></li>
					{% endif %}
					</ul>
				</section>
		</div>
		{% endif %}
	{% endblock %}

</div>

{% block rights %}
	{% include 'partials/rights.html.twig' %}
{% endblock %}