{% set feature_sets = page.collection({'items':'@self.modules', 'filter': {'type': 'modular/features'}}).order('header.position', 'asc') %}
{% for feature_set in feature_sets %}
	{{ feature_set.content|raw }}
{% endfor %}
